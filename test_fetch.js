const { createClient } = require('@supabase/supabase-js'); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY); async function run() { let allProjects = []; let mapPage = 0; let fetchMoreMap = true; while (fetchMoreMap) { const { data } = await supabase.from('projects').select('*').range(mapPage * 1000, (mapPage + 1) * 1000 - 1); if (data && data.length > 0) { allProjects = [...allProjects, ...data]; mapPage++; if (data.length < 1000) fetchMoreMap = false; } else { fetchMoreMap = false; } } console.log(allProjects.length); } run();
